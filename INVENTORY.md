# Inventory

Last full sweep: 2026-05-09 by Inventory Keeper. Post-rewrite re-sweep against ARCHITECTURE.md, RUNBOOK.md, README.md, refresh.yml, ci.yml, scripts/, tests/, docs/.

## Resolved

| ID  | File:Line | Category | Severity | Resolution | Reference |
|-----|-----------|----------|----------|------------|-----------|
| 001 | scripts/fetch-data.js, .github/workflows/refresh.yml:35-41 | auth | must-fix | Fixed. Workflow mints an installation token via `actions/create-github-app-token@v3` from `vars.PYVISTA_DASHBOARD_APP_ID` + `secrets.PYVISTA_DASHBOARD_APP_PRIVATE_KEY` and exports as `GH_TOKEN`. Fetcher is auth-mechanism agnostic. | refresh.yml:35-41,53-56 |
| 002 | docs/orgs/pyvista/, scripts/fetch-data.js:480-483 | dx | must-fix | Fixed. `docs/orgs/pyvista/index.html` committed; both `fetch-data.js` and `generate-pages.js` default `GH_ORGS` to `pyvista` when unset. config/orgs/pyvista.json shipped. | fetch-data.js:480-483, generate-pages.js:23-30 |
| 003 | scripts/fetch-data.js:205-314, docs/orgs/ORG_TEMPLATE/index.html | ux | must-fix | Fixed. `fetchWatchedRuns` queries `/actions/workflows` + `/actions/runs` per Tier 0 repo. `data.json` carries `ci.worst_conclusion` + `ci.watched[]`. Template renders CI chips, status pill, critical_alerts banner, and a Tier 0 card grid pinned at top. | fetch-data.js:231-314, ORG_TEMPLATE/index.html:721-780 |
| 004 | config/orgs/pyvista.json, scripts/fetch-data.js:62-69 | ux | must-fix | Fixed. Four-tier system: `classifyTier` reads `config.tiers["0"|"1"|"2"]`, archived repos are tier 3. Template renders Tier 0 as cards, Tiers 1/2/3 as tables (3 hidden when empty). Tier classification covered by 9 unit tests. | fetch-data.js:62-69, ORG_TEMPLATE/index.html:609-613 |
| 005 | README.md | docs | must-fix | Fixed. README has no `willingc.github.io` references and points at the pyvista deployment. | README.md |
| 006 | README.md:102, scripts/fetch-data.js:476 | docs | must-fix | Fixed. README now uses `GH_TOKEN=<token> node scripts/fetch-data.js` and documents `GH_ORGS=<org>` for non-default orgs. Script accepts only `GH_ORGS` and defaults to `pyvista`. | README.md:100-110, fetch-data.js:476-483 |
| 007 | README.md:36-58 | docs / auth | must-fix | Fixed. README step 1-2 walk through GitHub App registration with the read-only permission table and explicitly state PATs are disallowed. | README.md:36-58 |
| 008 | scripts/fetch-data.js:439-457, docs/orgs/ORG_TEMPLATE/index.html:933-966 | ci / ux | should-fix | Fixed. Per-repo and per-workflow errors collected into `data.json.fetch_errors`; whole-repo enrichment failure produces `ci.worst_conclusion: "fetch_error"` plus `error` field. Template surfaces this in the issues panel and yellow status pill. | fetch-data.js:367-376,439-457 |
| 012 | package.json | dx | should-fix | Fixed. `package.json` declares `engines.node >=20`, scripts `fetch`, `generate`, `lint`, `format`, `test`, and pins octokit + biome. `package-lock.json` committed. | package.json:1-25 |
| 013 | tests/fetch-data.test.js | tests | should-fix | Fixed. 48 unit + smoke tests via `node --test`: `classifyTier`, `worstConclusion`, `buildAlerts`, `runWithConcurrency`, `fetchPRCount` (link header parsing), `fetchWatchedRuns`, `fetchOrg` smoke. Run in CI on every push/PR. | tests/fetch-data.test.js, ci.yml:34-35 |
| 014 | biome.json | lint | should-fix | Fixed. Biome lint + format configured; `npm run lint` runs in `ci.yml`. | biome.json, ci.yml:31-32 |
| 015 | biome.json | lint | should-fix | Fixed. Biome formatter configured (double quotes, semicolons, trailing commas, 100 col). `npm run format` available. | biome.json:21-29 |
| 019 | scripts/fetch-data.js:181-185,199-202 | other | should-fix | Fixed. `fetchPRCount` and `fetchLatestRelease` now `console.warn` with org/repo and message; `fetchLatestRunForWorkflow` returns `__error` so per-run failures surface as `conclusion: "fetch_error"` in `data.json.fetch_errors`. | fetch-data.js:181-203,225-228 |
| 020 | scripts/fetch-data.js:128-148 | other | should-fix | Fixed. Octokit configured with `@octokit/plugin-throttling` and `@octokit/plugin-retry`. Primary and secondary rate-limit handlers retry up to 2 times and log to stderr. | fetch-data.js:128-148 |
| 021 | .github/workflows/refresh.yml:18-20 | ci | should-fix | Fixed. `concurrency: { group: "pages", cancel-in-progress: false }` declared. | refresh.yml:18-20 |
| 022 | .github/workflows/refresh.yml:70-82 | ci | should-fix | Fixed. On `failure()` the workflow opens or comments on a `dashboard-failure`-labeled tracking issue. RUNBOOK.md documents the recovery path. | refresh.yml:70-82, RUNBOOK.md |
| 023 | .github/workflows/refresh.yml | ci | should-fix | Fixed. Workflow no longer pushes to git. `docs/` is uploaded as a Pages artifact and deployed via `actions/deploy-pages@v4`; `main` can be branch-protected. | refresh.yml:61-68 |
| 024 | .github/workflows/refresh.yml | ci | should-fix | Fixed. No commit, so no `[skip ci]`. Pages deploy is via artifact upload. | refresh.yml |
| 025 | .github/workflows/refresh.yml | ci / auth | should-fix | Fixed. Workflow uses `pages: write` + `id-token: write` for `deploy-pages`; only `issues: write` is granted on the `GITHUB_TOKEN` for the failure-issue step. App token never persists past job. | refresh.yml:11-15 |
| 026 | README.md:70-78 | docs | should-fix | Fixed. README step 4 explicitly tells the user to set Pages source to "GitHub Actions" and not "Deploy from a branch". | README.md:70-78 |
| 031 | docs/orgs/ | drift | should-fix | Fixed. Only `docs/orgs/ORG_TEMPLATE/` and `docs/orgs/pyvista/` exist. The unrelated demo orgs were removed. | docs/orgs/ |
| 035 | README.md | docs | could-fix | Fixed. README references only per-org `docs/orgs/<org>/data.json`; `docs/index.html` redirects to `orgs/pyvista/`. | docs/index.html, README.md |
| 036 | .gitignore | dx | could-fix | Fixed. `.gitignore` covers `node_modules/`, `.env`, `*.log`, `.DS_Store`. | .gitignore |
| 037 | .github/CODEOWNERS, .github/dependabot.yml | docs | could-fix | Partially fixed. `CODEOWNERS` requires `@pyvista/maintainers` on workflows/scripts/config; `dependabot.yml` enables weekly grouped npm + actions bumps. LICENSE and CONTRIBUTING.md still absent — see open item N1. | .github/CODEOWNERS, .github/dependabot.yml |
| 042 | scripts/fetch-data.js:334 | other | could-fix | Fixed. `default_branch` propagated into per-repo data and used for `fetchWatchedRuns` branch filter. | fetch-data.js:334,360 |
| 010 | README.md:147-173 | docs / drift | should-fix | Fixed. Project-structure tree reflects current layout: ORG_TEMPLATE, pyvista/, fetch-data.js, ci.yml, refresh.yml, biome.json, package.json. | README.md:147-173 |
| 011 | docs/index.html | docs / dx | should-fix | Fixed. Root `docs/index.html` is a meta-refresh redirect to `orgs/pyvista/` and is documented in the README. | docs/index.html, README.md:78 |
| 017 | docs/orgs/ORG_TEMPLATE/index.html | dx | should-fix | Deferred (see D1). |
| 027 | docs/orgs/ORG_TEMPLATE/index.html:503-518 | ux / dx | should-fix | Deferred (see D2). |
| 030 | scripts/fetch-data.js:459-465 | other | should-fix | Deferred (see D3). |

## Open

| ID  | File:Line | Category | Severity | Owner | Acceptance criteria | Notes |
|-----|-----------|----------|----------|-------|---------------------|-------|
| O1  | ARCHITECTURE.md:50-53 | drift | RESOLVED | lead | Schema block now lists `unclassified_repos: string[]` and `fetch_errors: { repo, endpoint, message }[]`. Frontend and backend agree on shape. | Closed in final-pass commit. |
| O2  | README.md:127-141, RUNBOOK.md:48-62 | drift | RESOLVED | lead | README and RUNBOOK now describe `watched_workflows` as a top-level object keyed by repo name and instruct users to append full workflow paths matched against the workflow `path` field. | Closed in final-pass commit. |
| O3  | .nvmrc | drift | RESOLVED | lead | `.nvmrc` pinned to Node 20 at the repo root. Workflows continue to pin `node-version: "20"` directly. | Closed in final-pass commit. |
| O4  | scripts/generate-pages.js | tests | should-fix | lead | `generate-pages.js` has zero tests. Failure modes uncovered: missing template, multi-org expansion, idempotent overwrite of an existing file. Add at least three `node --test` cases that drive the script via `child_process` against a tmp dir or by importing helpers (the script currently has no exported helpers — minor refactor needed). | Test glob `tests/*.test.js` will pick up new file automatically. |
| O5  | docs/orgs/ORG_TEMPLATE/index.html:575 | dead code | could-fix | lead | `CI_RANK` table contains an unreachable key `cancelled_: 9` (trailing underscore). It is never read because no code path produces the string `"cancelled_"`. Either delete the entry or fix the typo to whatever was intended. | Search for the literal returns no other hits. |
| O6  | docs/orgs/ORG_TEMPLATE/index.html, docs/ | ux | could-fix | lead | No favicon. Browsers request `/favicon.ico` and 404. Acceptance: PyVista favicon committed at `docs/favicon.ico` and referenced via `<link rel="icon" href="favicon.ico">` from both `docs/index.html` and the template. | Was item 16 in the previous inventory; not addressed in the rewrite. |
| O7  | .github/workflows/refresh.yml | ci | could-fix | lead | Workflow has no status badge in README. Acceptance: README adds `![Refresh](…/refresh.yml/badge.svg)` near the title. | Was item 38; minor. |
| N1  | repo root | docs | could-fix | lead | No `LICENSE` and no `CONTRIBUTING.md`. CODEOWNERS and dependabot are present, but a public PyVista repo without a license cannot be reused legally. Acceptance: LICENSE chosen and committed (PyVista convention is MIT); CONTRIBUTING.md describes the dev loop (`npm ci`, `npm test`, `npm run lint`). | Carved out of old item 37 because the other half landed. |
| N2  | docs/orgs/ORG_TEMPLATE/index.html:548 | ux | could-fix | lead | Footer link `<a href="../../actions">` resolves to `/<repo>/actions` only when the page lives at `/<repo>/orgs/<org>/`. With Pages deploy this is fine in production, but it 404s for logged-out visitors and breaks under any non-default base path. Acceptance: link constructed from `json.org` + a configured `dashboard_repo` slug, or replaced with a link to the dashboard repo's commit history. | Was item 29; persists. |
| N3  | docs/orgs/ORG_TEMPLATE/index.html:706,743 | ux | could-fix | lead | `release.tag` is rendered into a span with no max-width; long tags (e.g. `v1.2.3-rc.1+build.20260101.1234`) widen the table column and trigger horizontal scroll. Acceptance: `.release-tag` gets `max-width` + `overflow:hidden;text-overflow:ellipsis`; full tag goes in `title`. | Was item 40. |
| N4  | scripts/fetch-data.js:132 | other | could-fix | lead | User-Agent hardcoded as `pyvista-org-dashboard/1.0`. `package.json` is at `1.0.0` so the literal is right today, but they will drift. Acceptance: read `name` + `version` from `package.json` (relative `import` with `assert { type: "json" }`) and build the UA at runtime. | Was item 34. |
| N5  | scripts/fetch-data.js:351 | other | should-fix | lead | `open_issues = max(0, repo.open_issues_count - prCount)` is still an approximation and can mis-clamp when the PR count is stale or paginated zero (private PRs, secondary rate limit returning 0). Either switch to `/search/issues?q=repo:org/repo+is:issue+is:open` for an authoritative count, or surface `open_issues_approx: true` in `data.json` and tooltip it in the UI. | Was item 18; the fetch errors infrastructure now exists to surface this honestly. |
| N6  | docs/orgs/ORG_TEMPLATE/index.html | ux | could-fix | lead | Inline `oninput="renderAll()"` and `onchange="renderAll()"` handlers (lines 503, 504, 513, 517, 518) still rely on `renderAll` being a global. Blocks adding a strict CSP without `unsafe-inline`. Acceptance: replace with `addEventListener` registrations inside the existing `<script>` block; add CSP `<meta>` without `unsafe-inline`. | Lower priority because all JS is same-origin inline; coupled with D1. |
| N7  | docs/orgs/ORG_TEMPLATE/index.html, scripts/generate-pages.js:46 | drift | could-fix | lead | `generate-pages.js` writes the template byte-for-byte into `docs/orgs/<org>/index.html`. If a maintainer hand-edits an org page (debugging, A/B test), the next workflow run silently overwrites it. Acceptance: either land item D1 (per-org pages become a 5-line shim) or have `generate-pages.js` warn when target file differs from template before overwriting. | Pages-artifact deploy means committed per-org HTML is no longer required for publication; could be moved to a runtime-only path. |

## Deferred

| ID  | Date | Rationale | Revisit when |
|-----|------|-----------|--------------|
| D1 (was 17, 09) | 2026-05-09 | Extracting CSS + JS to `docs/assets/dashboard.{css,js}` and turning per-org `index.html` into a shim is a clean refactor, but the current single-file template is 1031 lines, has zero external assets, deploys atomically as a Pages artifact, and is covered by the existing manual-QA loop. The refactor adds a build step or asset-path discipline that the project does not otherwise need. Trade-off: ~330 inline JS+CSS lines per org page (currently one org), times the cost of one find-replace whenever the template changes. Accept until a second org is added or until a CSP is required. | A second org is added to `GH_ORGS`, OR CSP support becomes a hard requirement, OR template grows past 1500 lines. |
| D2 (was 27) | 2026-05-09 | Replacing inline `oninput`/`onchange` handlers with `addEventListener` is straightforward but only meaningful behind a CSP. No CSP is configured today. Tracked as N6 for the day CSP lands. | CSP `<meta>` is added (see N6). |
| D3 (was 30) | 2026-05-09 | `data.json` is no longer committed. It is uploaded as a Pages artifact every run and never lands in git, so churny `fetched_at` no longer creates noisy diffs. The original concern was made obsolete by the deploy-pages migration. | Workflow ever reverts to committing `data.json` to a branch. |
| D4 (was 32, 33, 41, 43) | 2026-05-09 | Manual theme toggle, `Intl.RelativeTimeFormat` for `relInfo`, fetching `topics`, and surfacing license SPDX are all true could-fix items but none of them block v1.0 of the dashboard or block any user task observed in RUNBOOK.md. Cost is real (more API calls, more UI surface, more tests); benefit is incremental polish. | A user request for filtering by topic or license, OR a complaint about rounded relative dates, OR the dashboard adds a settings panel. |
| D5 (was 39) | 2026-05-09 | The frontend now uses `escapeHtml()` everywhere user-controlled data (`r.name`, `r.description`, `r.release.tag`, `a.workflow`, `a.conclusion`, etc.) is interpolated into `innerHTML`. The remaining defense-in-depth ask was to migrate to DOM construction via `textContent`/`createElement`. That is a large refactor (~30 interpolation sites) for marginal additional safety on top of `escapeHtml`. | A real injection class is found, OR template moves to a templating library. |
| D6 (was 28) | 2026-05-09 | `.nvmrc` not committed. `package.json` already pins `engines.node: ">=20"` and both workflows pin `node-version: "20"`, so the contract is enforced where it matters. `.nvmrc` is contributor convenience, not correctness. Tracked under O3 because the README local-dev section still does not state the version. | O3 is closed; at that point `.nvmrc` becomes the cheap fix. |

Tally: 18 entries · 0 must-fix open, 2 should-fix open (O4 generate-pages tests, N5 open-issues approximation), 8 could-fix open, 6 deferred with measured rationale, 31 resolved including final-pass closures of O1, O2, O3.
